name: Liquibase Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Java
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Download Liquibase + JDBC Driver
      run: |
        curl -L https://github.com/liquibase/liquibase/releases/download/v4.27.0/liquibase-4.27.0.zip -o liquibase.zip
        unzip liquibase.zip -d liquibase
        mkdir -p liquibase/lib
        curl -L https://github.com/microsoft/mssql-jdbc/releases/download/v12.6.0/mssql-jdbc-12.6.0.jre17.jar -o liquibase/lib/mssql-jdbc.jar
        chmod +x liquibase/liquibase

    - name: Run Liquibase update
      run: |
        liquibase/liquibase \
          --url="${{ secrets.DB_URL }}" \
          --username="${{ secrets.DB_USER }}" \
          --password="${{ secrets.DB_PASS }}" \
          --changeLogFile=db.changelog-master.xml \
          --classpath=liquibase/lib/mssql-jdbc.jar \
          update
